<h2 class="page-header">Recently Added Books</h2>

<div>
    <% books.forEach(book => { %>
    <div>
        <img src="<%= book.coverImagePath%>" height="150" width="100">
    </div>
    <% }) %>
</div>